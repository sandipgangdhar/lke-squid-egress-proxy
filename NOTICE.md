# Operational notice

This pattern provides explicit HTTP/HTTPS proxying. It is not a managed NAT Gateway and does not transparently route arbitrary protocols. Validate application proxy support, egress IP requirements, capacity, security controls, and failure behavior before production use.
